const add=(a,b)=>{
    return a + b;
}
const subtract=(a,b)=>{
    return a - b;
}
// module.exports={add, subtract};
// module.exports is working but the exports is not working because the references is broken
// exports ={add, subtract};